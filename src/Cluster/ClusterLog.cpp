#include "ClusterEntity.h"
#include "ClusterEntityLeader.h"
#include "ClusterEntityFollow.h"
#include "ClusterLog.h"

namespace cluster
{
    void ClusterLog::init()
    {

    }

    void ClusterLog::addLog(uint32 term, uint32 index, uint32 status, uint32 syncNum)
    {

    }

    void ClusterLog::updateLog(uint32 term, uint32 index, uint32 status, uint32 syncNum)
    {

    }

    uint32 ClusterLog::getNodeNum()
    {
        return 0;
    }
}
